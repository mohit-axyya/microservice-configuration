spring:
  application:
    name: CONFIG-PROD
eureka:
  client:
    fetch-registry: true
    fetch-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true
